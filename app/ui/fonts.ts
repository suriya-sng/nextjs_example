import { Kanit } from 'next/font/google';
 
export const kanit = Kanit({
    weight: ['200'], // Valid weights should be strings and can include multiple values as an array.
    subsets: ['latin'], // Ensure to specify subsets, e.g., 'latin'.
  });